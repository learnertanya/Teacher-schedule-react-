import React from 'react';
import '../styles/Footer.css'

const Footer = () => {
  return (
    <footer className = "footer">
      {/* Add your footer content, including dummy links to social platforms and other pages */}
    </footer>
  );
};

export default Footer;
